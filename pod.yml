apiVersion: v1
kind: Pod

metadata:
  name: my-nginx-Pod
  labels : 
    app: web
    tier : frontend

spec :
  containers :
  - name: nginx-container
    image : nginx:latest
    ports:
      - containerPort: 80
